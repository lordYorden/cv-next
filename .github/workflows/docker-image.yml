name: Docker Image CICD

on:
  push:
    branches: [ "feature/devops" ]
  pull_request:
    branches: [ "feature/devops" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build ./cv_next --file ./cv_next/Dockerfile --tag assaf765/cv-next-repo:fromGit
    
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: assaf765
        password: dckr_pat_2ifZ_mDGwigb_vOVLYC7-_E-uRY
    - name: pushing the image
      run: docker push assaf765/cv-next-repo:fromGit
    - name: finishing the creating image
      run: echo "finished building and integrating the code in the image"

    - name: CD PIPE - rebuilding the app
      run: "curl -H \"Content-Type: application/json\" -H \"Authorization: Bearer dop_v1_392c55bfd2413dfb56ad37c30d8afc3833bfa9e0656bda64b0c581c6ceb6427f\" \"https://api.digitalocean.com/v2/apps/3e2f1be4-4a2e-4e4d-afca-16a64dabb8c6/deployments\" -XPOST -d '{\"force_build\": true}'"

